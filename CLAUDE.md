# artpsy-web

**자율 연습 프로토타입이다.** 실제 수주 건이 아니고 의뢰인도 없다 — 공개된 외주 공고를
요구사항 샘플로 삼았을 뿐이므로, 클라이언트 산출물처럼 다루지 않는다.

허브: `../MyPrivate`. 운영 규칙·역할·핸드오프는 거기 `CLAUDE.md`와 `.ai/`를 읽는다.
팀: `artpsy` — 설정은 허브 `teams/artpsy.yml`.

이슈 관리는 GitHub Issues로 한다 — 절차는 허브 `.ai/workflows/issue-flow.md`.
라벨이 다음 담당을 지정하고, 카드는 홉 하나를 배달한다.

요구사항과 리스크, 스택 결정 근거는 허브 `md/` vault에 있다 (이 repo에는 없다).

## 이 프로젝트 고유

- base 브랜치: `main`
- 개발: `npm run dev` · 빌드: `npm run build` · 확인: `npm run preview`
- 린트: 아직 없음 (Phase 1 범위 밖)

## 현재 Phase

**Phase 1 — 프론트엔드 프로토타입. 종료됨** (2026-08-13, `e72f1ae`). Vite + Lenis + GSAP
ScrollTrigger. 이 프로젝트에서 가장 위험한 것은 CMS 배선이 아니라 **하이엔드 인터랙션과
성능**이라, 그것부터 실제로 돌려서 검증했다. 이슈 4건 전부 종결.

종료 지점은 **`phase-1` 태그**로 고정돼 있다. Phase 2와 대볼 기준선은 전부 **허브
`tools/perf-harness/baselines/`** 에 있다 (repo 아닌 이유: 그걸 읽는 하네스가 거기 있고,
전면 캡처는 PR diff에서 눈으로 볼 수 없어 스크립트 대조가 유일한 사용법이다).

- 수치 29건 — 라운드별 성능 원자료
- `dist-manifest-phase-1.json` — 파일별 크기·sha256, 재빌드 동일성 대조용
- `visual-phase-1/` — 전면 캡처 4폭(390·768·1440·1600), `prefers-reduced-motion`

**1440만 보고 판정하지 않는다.** 그 폭에서는 `wideSize`와 콘텐츠 폭이 우연히 겹쳐 갈리지
않는다. 근거와 사용법은 하네스 README §5.5.

**Phase 2 — WordPress 블록 테마 이식. 종료됨** (2026-08-14, `d4b217a`, 태그 `phase-2`).
Phase 1의 토큰(`src/styles/tokens.css`)을 `theme.json`으로 매핑하는 것이 이식의 축이었다.
커밋 35 · 머지된 PR 20 · 테스트 0 → 315. 회고는 허브
`md/project/artpsy/analysis/20260814_phase2-회고.md`가 정본이다.

- 테마: `theme/artpsy/` · 구동 설정: `.wp-env.json` (둘 다 Phase 1 트리와 공존한다)
- 구동은 `@wordpress/env`로 하고 WordPress는 컨테이너의 PHP 8.x를 쓴다 — 호스트 PHP 7.4는
  대비책이고 `mbstring`·`zip`이 빠져 있다(20.04 universe/main 버전 핀 충돌).
- `docker` 그룹이 로그인 세션에 아직 안 붙었으면 `sg docker -c "..."` 로 감싼다.
  영구 해결은 완전 로그아웃 후 재로그인이다.

## 에디터에서 이미지 블록을 클릭해도 된다 (2026-08-15, `#27`로 닫힘)

**이 절은 경고에서 기록으로 바뀌었다.** `d4b217a`에서는 `core/image` 셋이 무효라
클릭하면 "블록 복구"가 떴고, 누르면 `class="hero__media"`가 사라져 히어로 CSS가 통째로
풀렸다. `#27`이 `<img>`의 `class`/`width`/`height`를 빼서 저장 마크업을 유효하게 만들고
걸이를 구조 선택자로 옮겼다. **지금은 셋 다 유효하고 편집자가 이미지를 바꿔도 된다.**

남는 사실 하나: **"블록 복구"는 저장된 마크업을 `save()` 출력으로 다시 쓴다.** 저장 마크업이
`save()`와 다른 블록을 만들면 그 차이는 편집자의 클릭 한 번으로 사라진다.

## 성능 — 예산을 넘긴 채로 닫혔고, **그 뒤로 안 쟀다**

아래 표는 **`d4b217a`(Phase 2 종료) 시점이다.** Phase 3은 성능을 판정에서 뺐고
**한 번도 재지 않았다** — 그 사이 템플릿이 하나에서 아홉으로, 라우트가 하나에서 여덟으로
늘었다. **그러니 이 표를 현재 값으로 읽지 마라.** 지금 값은 아무도 모른다.

`d4b217a`가 `perf-budget.json`의 `LCP max=2500`을 넘긴다. 아래는 전부 p75다.

| | Phase 1 최종 (`perf4-*`) | Phase 2 (`d4b217a`) | 예산 |
|---|---|---|---|
| LCP desktop | **1,400** (1,380~1,440) | **6,488** (6,472~6,492) | 2,500 **FAIL** |
| LCP mobile | **1,476** (A+B 10회) | **5,088** | 2,500 **FAIL** |
| LCP 후보 | `h1.display` | **`img.hero__media`** | — |
| loadMs desktop | 8,694 | **6,432** | 10,000 PASS |
| CLS · INP desktop | 0.003 · 16 | 0.002 · 8 | PASS |

**로드는 2.3초 빨라졌는데 LCP는 4.6배다.** 후보가 바뀌었기 때문이고 **그 원인을 모른다.**

**Phase 1 기준선은 `perf4-*`다. `perf3-*`이 아니다** — 3차는 `preload`가 켜져 있고 LCP가
FAIL이던 중간 라운드이고, 4차가 그 `preload`를 걷어내고 전 항목을 통과시킨 최종본이다.
모바일 4차는 배치가 둘(`perf4-mobile.json` + `-B`)이고 **합쳐야 성립한다**(A만 쓰면 p75가
1,784로 더 나빠 보인다). 근거는 하네스 README §5.3/§5.4.

**전송량을 Phase 1과 그냥 비교하면 안 된다** — 995,484 대 1,403,143은 저널 썸네일이
24장 대 2장이라 "줄었다"가 아니라 **"22장이 없다"**다. 내용 등가로 맞추면 예산을 넘긴다.

기준선은 허브 `tools/perf-harness/baselines/*-phase-2-d4b217a.json` 4건.

## `layout: constrained`를 grid/flex 칸 안에 쓰지 않는다

**규칙으로 적는다. 사례를 열거하면 새 사례가 생길 때마다 아무도 못 본다** — 히어로가
정확히 그랬다. 아래 둘은 그 규칙이 어디서 나왔는지의 기록이지 목록이 아니다.

`constrained`는 자식을 `contentSize`로 가둔다. 호스트가 그리드/플렉스 칸이면 칸 폭과
자식 폭이 갈리고, **그 차이는 폭이 우연히 겹치는 동안 안 보인다.**

- **히어로 — 닫혔다** (Phase 3, `HERO-ALIGN`). eyebrow와 메타가 881이 아니라 544였고
  왼쪽 선이 h1(279)과 448로 갈렸다. **편집자 조작 없이 첫 화면에 보이던 것**이라
  "언젠가 드러난다"가 아니었다 — `#28`이 로고라는 비교 기준을 놓기 전까지 볼 것이 없었을 뿐이다.
- **`.journal__item` — 닫혔다** (Phase 3, `JOURNAL-CONSTRAINED`). 호스트가 572인데 자식 셋이
  전부 544였다. `.card`가 368이라 화면에는 안 보였고 **편집자가 카드를 하나 지워 572가 될 때**
  드러날 것이었다 — 히어로와 달리 조작이 조건이었다.

## 이식 결함은 전부 닫혔다 (2026-08-15)

Phase 2가 남긴 셋이 Phase 3에서 다 닫혔다. **목록이 아니라 기록이다** — 위의
`constrained` 규칙이 어디서 나왔는지가 여기 있다.

| 결함 | 닫은 것 | 무엇이 바뀌었나 |
|---|---|---|
| `core/image` 셋 무효 | `#27` | 저장 마크업 유효 · 걸이가 구조 선택자로 |
| 히어로 이미지 박스 975.94 | `#27` (딸려서) | 1080 · 하단 틈 0. 새 선택자가 코어를 이겼다 |
| 히어로 안쪽 `constrained` | `#41` | eyebrow·메타가 544→881, 세로선 셋이 둘로 |
| `.journal__item` `constrained` | `#42` | 자식 544→572. 썸네일이 카드 폭까지 찬다 |

**`#42`는 "보이는 것이 안 바뀐다"로 판정을 잡았다가 틀렸다.** 실제로는 썸네일이
544×408→572×429로 커졌고 그것이 개선이었다. 눈에 안 보일 것이라고 **예측한** 것이
틀린 것이지 고침이 틀린 것이 아니다 — 판정에 값을 미리 박지 않는 이유가 이것이다.
근거는 허브 `md/project/artpsy/architect/20260815_phase3-계획.md` §2.2.3.

## 작업 트리는 공유 자원이다

`.wp-env.json`이 작업 트리를 컨테이너에 **bind mount** 한다. **체크아웃이 곧 브라우저가
받는 CSS·PHP다** — 성능 측정 중에 커밋하면 회차별 입력이 갈린다(실제로 한 라운드를 버렸다).
측정 중에는 커밋하지 않고, 브랜치 작업은 `git worktree`로 딴 경로에서 한다.

## Phase 3 — 기능 완성. **종료됨** (2026-08-15, `phase-3` 태그)

사용자 결정(2026-08-15): *"모든 기능 개발이 Phase 3의 목표다. 나머지는 이후. 성능과
사소한 오류는 우선 무시한다. 목표로 한 것이 동작하는 것을 먼저 만든다."*

계획은 허브 **`md/project/artpsy/architect/20260815_phase3-계획.md`가 정본이다.**
범위선은 **익명/로그인** — 로그인 없이 보이는 것 전부가 안이고 LMS·커머스·회원은 밖이다.

계획 12개 전부 닫혔다 — 블록 유효성 · smoke · 헤더·푸터 · 페이지 다섯 · 저널 포스트 타입 ·
폼 마크업 · 폼 처리 · FAQ·예약·모바일 내비·권한 · 팝업 · SEO · 관리자 화면 · 매뉴얼.
**머지된 PR 21**(`#27`~`#48`) · **테스트 315 → 644**.
회고는 허브 `md/project/artpsy/analysis/20260815_phase3-회고.md`가 정본이다.

장치 넷이 남는다 — **smoke**(L2·L3) · **자산 404 검사** · **AX 트리 검사** · 폼 자리 힌트.
`npm run smoke`가 그 입구다.

### 판정이 성능에서 세 층으로 바뀌었다

성능이 판정에서 빠진 자리를 이것이 채운다. **PR은 자기 층까지만 요구한다.**

```
L1 존재   파일이 그렇게 적혀 있나        vitest 텍스트 파싱      CI · 모든 PR
L2 도달   그 URL 이 200 이고 그 템플릿이 골렸나   npm run smoke   PR 별
L3 거동   보내면 남고, 남은 것이 보이나   smoke + wp db query     상태 바꾸는 PR
```

**200은 판정이 아니다.** 템플릿을 못 찾으면 폴백이 렌더되고 200이 나온다 —
템플릿마다 고유 표식(`tpl-*`)을 심고 그것으로 판정한다.

**템플릿 파일은 URL을 만들지 않는다.** WP는 쿼리를 먼저 풀고 템플릿을 나중에 고르므로
슬러그에 맞는 `page` 포스트가 있어야 한다. `smoke/seed-pages.mjs`가
`.wp-env.json`의 `lifecycleScripts.afterStart`에서 그것을 만든다 — **테마에는 없다.
산출물은 테마고 페이지는 고객 것이다.**

**다섯 페이지의 정본은 `smoke/pages.mjs` 하나다.** 템플릿·라우트 표·시드·헤더 내비가
전부 거기서 파생된다.

### 자리표시자인 것들 — 클라이언트가 채울 자리

지어낸 사실을 넣지 않기로 했다. **비어 있는 것이 의도다.**

**목록과 "어디서 채우나"는 `docs/20260815_관리자-매뉴얼.md` §9가 정본이다.** 여기에
다시 적지 않는다 — 두 곳에 적히는 순간 갈라지고, 갈라진 쪽이 어디인지는 아무도 안 본다.
그 목록은 템플릿에서 실제로 뽑은 것과 대조된다(`tests/manual.test.js`).

- 방문 통계는 **자체 최소 집계**다. 위젯에 한계가 문장으로 적혀 있다 —
  봇을 안 거르고, 캐시가 있으면 적게 잡히고, 외부 도구와 값이 다르다

### LCP 스파이크는 여전히 안 열렸다

*"왜 히어로가 LCP 후보가 됐나"* 는 Phase 4의 첫 홉으로 남아 있다. 원인을 모르는 채로
이미지를 줄이면 LCP가 내려갈지 다른 후보로 넘어갈지 예측이 안 선다.
**`LargestContentfulPaint.size`를 읽은 라운드가 아직 없다** — Phase 3에서 나온 값들은
전부 DOM 기하 실측이거나 그것에서 유도한 것이다.

## 지킬 것

- 시각적인 값은 전부 `src/styles/tokens.css`의 토큰으로 수렴시킨다. Phase 2에서 `theme.json`과
  1:1로 매핑되어야 하므로, 컴포넌트에 하드코딩된 색·간격이 남으면 그만큼 이식이 수작업이 된다.
- **Phase 3의 예산 면제는 끝났다** (2026-08-15). 다시 올릴 조건이었던
  *"기능 목록이 전부 L2/L3을 통과하고 페이지 수가 더 안 늘어날 때"* 가 성립했다.
  **다만 지금 대볼 기준선이 없다** — Phase 2 종료(`d4b217a`) 이후 한 번도 안 쟀고 그 사이
  템플릿이 아홉, 라우트가 여덟이 됐다. **그러니 다음 성능 변경의 첫 일은 고치는 것이 아니라
  재는 것이다.** 재기 전에는 무엇이 넘쳤는지 아무도 모른다.
  - 임계와 측정 조건(느린 4G가 정확히 무엇인지)은 `perf-budget.json`이 정본이다.
    값을 여기 두 벌로 적지 않는다 — 갈라지면 어느 쪽이 맞는지 아무도 모른다.
  - 그 파일 `required`의 키가 하나라도 없거나 `null`이면 FAIL이다. 임계 비교보다 먼저 본다.
    Phase 1에서 실제로 난 사고는 예산이 깨진 것이 아니라 측정기가 조용히 값을 안 낸 것이었다.
  - 세 지표만으로는 부족하다 — 전부 통과하면서 초기 로드 72초가 실측된 적이 있다.
    풀블리드 히어로는 LCP 후보에서 빠지므로 LCP가 이미지 비용을 담지 않는다.
    근거는 허브 `md/analysis/20260812_artpsy-phase1-성능실측.md`.
- `prefers-reduced-motion`을 무시하지 않는다. 심리상담 도메인이라 더 그렇다.
- JS 실패가 백지 화면이 되면 안 된다. `.js` 클래스만으로는 부족하다 — GSAP이 트윈 생성 시점에
  인라인 `opacity: 0`을 쓰므로, 초기화가 중간에 죽으면 클래스를 떼도 복구되지 않는다.
  초기화 실패는 `stopMotion()`으로 정적 상태까지 내린다.
