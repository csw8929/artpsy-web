{
  "profile": {
    "name": "Slow 4G + CPU 4x",
    "source": "tools/perf-harness/cdp.mjs — SLOW_4G",
    "latencyMs": 562.5,
    "downloadBytesPerSec": 188743.68000000002,
    "uploadBytesPerSec": 86400,
    "cpuThrottlingRate": 4,
    "note": "cdp.mjs 의 식을 그대로 계산한 값이다. latency = 150 * 3.75, download = (1.6*1024*1024)/8*0.9, upload = (750*1024)/8*0.9. download 의 끝자리는 오타가 아니라 부동소수점 결과 그대로다 — 188743.68 로 반올림해 적으면 cdp.mjs 의 상수와 === 로 같지 않다."
  },
  "aggregate": {
    "method": "p75",
    "minRuns": 5
  },
  "budgets": {
    "LCP": {
      "max": 2500,
      "unit": "ms"
    },
    "INP": {
      "max": 200,
      "unit": "ms"
    },
    "CLS": {
      "max": 0.1,
      "unit": ""
    },
    "loadMs": {
      "max": 10000,
      "unit": "ms"
    },
    "initial.total": {
      "max": 1500000,
      "unit": "bytes",
      "note": "1.5 MB (십진). 회고 §2 가 히어로를 3.3MB 로 쓰는데 그 파일이 3,312,600 바이트다 — 같은 문서가 MB 를 십진으로 쓴다 (결정 2026-08-14)."
    },
    "full.total": {
      "max": 4000000,
      "unit": "bytes",
      "note": "4.0 MB (십진). 위와 같다."
    }
  },
  "required": [
    "LCP",
    "INP",
    "CLS",
    "loadMs",
    "initial.total",
    "full.total"
  ],
  "baseline": {
    "tag": "phase-1",
    "dir": "tools/perf-harness/baselines/"
  }
}
